{
  "name": "zeno",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "tsc && tscpaths -p tsconfig.json -s src -o dist",
    "build:watch": "tsc --watch && tscpaths -p tsconfig.json -s src -o dist"
  },
  "type": "module",
  "dependencies": {
    "tscpaths": "^0.0.9"
  }
}